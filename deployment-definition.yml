apiVersion: apps/v1
kind: Deployment
metadata:
 name: test-rc
 labels:
   app: myapp
   type: front-end
spec:
  template:
    metadata:
      name: test
      labels:
        app: kat
        type: front-end
    spec:
      containers:
         - name: prabhu
           image: nginx:1.7.1
  replicas: 3
  selector:
        matchLabels:
                type: front-end
